<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.RecruitNewPeople.mapper.DepartmentMapper">
    <select id="getPkIdByStudentId" resultType="java.lang.Integer">
        SELECT pk_id
        FROM status
        WHERE id = #{id} AND status = 0
        LIMIT 1
    </select>

    <insert id="nextDepartment">
        INSERT INTO status (id, department_id, status)
        SELECT #{id}, volunteer_department, 0
        FROM volunteer
        WHERE id = #{id} AND level = '2'
        LIMIT 1
        ON DUPLICATE KEY UPDATE status = 0
    </insert>
    <update id="changeToPassStatus">
        UPDATE status SET status = 1 WHERE pk_id = #{pkId} AND id = #{id}
    </update>
    <update id="changeToNoPassStatus">
        UPDATE status SET status = 2 WHERE pk_id = #{pkId} AND id = #{id}
    </update>
    <update id="updateStatusToUnprocessed">
        UPDATE status SET status = 0 WHERE pk_id = #{pkId} AND id = #{id}
    </update>

    <select id="choosePassPerson" resultType="com.example.RecruitNewPeople.entity.pojo.Users">
        SELECT recruit_user.student_id, user_name, introduction, major, college, first_intention, second_intention, phone, gender, status.MessageStatus from recruit_user,status  WHERE department_id=#{departmentId} AND status.status=1 AND recruit_user.student_id = status.id;

    </select>
    <select id="chooseWillPassPerson" resultType="com.example.RecruitNewPeople.entity.pojo.Users">
        SELECT recruit_user.student_id, user_name, introduction, major, college, first_intention, second_intention, phone, gender, status.MessageStatus from recruit_user,status  WHERE department_id=#{departmentId} AND status.status=0 AND recruit_user.student_id = status.id;
    </select>
    <select id="chooseNotPassPerson" resultType="com.example.RecruitNewPeople.entity.pojo.Users">
        SELECT recruit_user.student_id, user_name, introduction, major, college, first_intention, second_intention, phone, gender, status.MessageStatus from recruit_user,status  WHERE department_id=#{departmentId} AND status.status=2 AND recruit_user.student_id= status.id
    </select>
    <select id="departmentExist" resultType="java.lang.Boolean">
        select  count(*) from department where department_id=#{departmentId};
    </select>
    <select id="userExist" resultType="java.lang.Boolean">
        select  count(*) from recruit_user where student_id=#{id}
    </select>
    <select id="selectMessageId" resultType="java.lang.Boolean">
        select count(*) from ReviceMessage where messageId = #{messageId};
    </select>
    <select id="getDepartment" resultType="com.example.RecruitNewPeople.entity.pojo.Department">
        select * from department;
    </select>


</mapper>
